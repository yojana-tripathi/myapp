var express = require('express');
var router = express.Router();

/* GET home page. */
/*router.get('/', function(req, res, next) {
 // res.render('index', { title: 'Express' });
 console.log(req)
 res.json({name:"yojana"});
});

router.get('/:da', function(req, res, next) {
    // res.render('index', { title: 'Express' });
    console.log(req.params.da)
    res.json({name:req.params.da});
   });
   

router.get('/:da-:ta', function(req, res, next) {
    // res.render('index', { title: 'Express' });
    console.log(req.params.da)
    res.json({name:req.params.da +" and "+req.params.ta+"are brother and sister"});
   });

router.get('/:da-:ta-ch', function(req, res, next) {
    // res.render('index', { title: 'Express' });
    console.log(req.params.ch)
    res.json({name:req.params.da +","+ req.params.ch +"and"+req.params.ta+"are friend"});
   });

module.exports = router;*/
